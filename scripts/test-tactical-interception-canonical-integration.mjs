import { assert, setup, takeStep } from "./tactical-charge-brace-test-helpers.mjs";
const miss = setup(); await takeStep(miss, { from: { x: 0, y: 0 }, to: { x: 1, y: 0 } }); const missed = await takeStep(miss, { from: { x: 1, y: 0 }, to: { x: 2, y: 0 }, result: { accepted: true, hit: false } });
assert.equal(missed.boundary.continuation.outcome, "continues"); assert.equal(missed.boundary.canonicalResult.request.tacticalSource, "brace-interception"); assert.equal(missed.boundary.canonicalResult.request.reactionDepth, 1);
const damaging = setup(); await takeStep(damaging, { from: { x: 0, y: 0 }, to: { x: 1, y: 0 } }); const hit = await takeStep(damaging, { from: { x: 1, y: 0 }, to: { x: 2, y: 0 }, result: { accepted: true, hit: true, damage: 7 } }); assert.equal(hit.boundary.continuation.outcome, "continues");
const knocked = setup(); await takeStep(knocked, { from: { x: 0, y: 0 }, to: { x: 1, y: 0 } }); const stopped = await takeStep(knocked, { from: { x: 1, y: 0 }, to: { x: 2, y: 0 }, result: { accepted: true, hit: true, stability: { prone: true } } }); assert.equal(stopped.boundary.chargeStopped, true); assert.equal(stopped.boundary.continuation.reason, "charger-knocked-down");
const forcedOff = setup(); await takeStep(forcedOff, { from: { x: 0, y: 0 }, to: { x: 1, y: 0 } }); const offPath = await takeStep(forcedOff, { from: { x: 1, y: 0 }, to: { x: 2, y: 0 }, result: { accepted: true, hit: true, stability: { displacementFeet: 5 } }, authoritativePosition: { x: 1, y: 1 } }); assert.equal(offPath.boundary.continuation.reason, "charger-forced-off-path");
const forcedAlong = setup(); await takeStep(forcedAlong, { from: { x: 0, y: 0 }, to: { x: 1, y: 0 } }); const alongPath = await takeStep(forcedAlong, { from: { x: 1, y: 0 }, to: { x: 2, y: 0 }, result: { accepted: true, hit: true, stability: { displacementFeet: 5 } }, authoritativePosition: { x: 2, y: 0 } }); assert.equal(alongPath.boundary.continuation.outcome, "disrupted"); assert.equal(alongPath.boundary.commitStep, false);
const armorOnly = setup(); await takeStep(armorOnly, { from: { x: 0, y: 0 }, to: { x: 1, y: 0 } }); const armor = await takeStep(armorOnly, { from: { x: 1, y: 0 }, to: { x: 2, y: 0 }, result: { accepted: true, hit: true, armorContact: { outcome: "absorbed" }, shield: { intercepted: true } } }); assert.equal(armor.boundary.continuation.outcome, "continues");
const rejected = setup(); await takeStep(rejected, { from: { x: 0, y: 0 }, to: { x: 1, y: 0 } }); const admissionRejected = await takeStep(rejected, { from: { x: 1, y: 0 }, to: { x: 2, y: 0 }, result: { accepted: false, reason: "ownership-rejected" } }); assert.equal(admissionRejected.boundary.commitStep, true); assert.equal(rejected.runtime.bracesByActor.get("bracer").state, "held");
console.log("tactical interception canonical integration: 15/15 passed");
